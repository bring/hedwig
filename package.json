{
  "name": "hedwig",
  "version": "0.0.1",
  "description": "Bring Design System",
  "main": "index.js",
  "scripts": {
    "css:build": "postcss --replace ./src/**/*.css -o ./build/main.min.css -u autoprefixer -u postcss-cssnext -u cssnano",
    "css:watch": "postcss --replace ./src/**/*.css -o ./build/main.css -u autoprefixer -u postcss-cssnext --watch",
    "js:build": "babel-compile -p es2015 src:build",
    "serve": "node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^6.7.7",
    "babel": "^6.23.0",
    "babel-compile": "^2.0.0",
    "babel-preset-es2015": "^6.24.0",
    "catalog": "^2.5.3",
    "cssnano": "^3.10.0",
    "express": "^4.15.2",
    "postcss-cli": "^3.0.0",
    "postcss-cssnext": "^2.10.0"
  }
}
